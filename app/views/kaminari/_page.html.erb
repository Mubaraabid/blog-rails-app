<span class="page<%= ' current' if page.current? %>">
  <%=
    link_to_unless page.current?,
                   page,
                   url,
                   {
                     remote: remote,
                     rel: page.rel,
                     class: 'text-green-100 bg-white hover:text-green-200'
                   }
  %>
</span>
